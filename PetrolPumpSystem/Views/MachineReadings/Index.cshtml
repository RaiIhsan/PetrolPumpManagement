@model IEnumerable<PetrolPumpSystem.Models.tblMachineReading>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="SubPageHeader">
    <div class="row">
        <div class="col-sm-12">
            <h4 style="float:left;">Machines Readings</h4>
            <h4 style="float:right;margin-right:2em;">Total Suppliers: @Session["totalSuppliers"]</h4>
        </div>
    </div>
</div>
<br />
<p class="subPageMargin">
    @*@Html.ActionLink("Add New", "Create")*@
    <input type="button" class="btn btn-success" value="Add New" onclick="location.href='@Url.Action("Create", "MachineReadings")'" />

</p>
<div class="container table-responsive">
    <table class="table table-responsive table-striped " id="myTable">
        <thead>
            <tr class="tblHeader">
                <th>
                    Start Reading
                </th>
                <th>
                    Ending Reading
                </th>
                <th>
                    Date
                </th>
                <th>
                    Machine
                </th>
                <th>Actions</th>
            </tr>
            </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr class="tblbody">
                    <td>
                        @Html.DisplayFor(modelItem => item.StartReading)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.EndingReading)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ReadingDate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Machine.MachineName)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.MReadingId }, new { @class = "btn btn-primary btn-sm" })


                        @*@Html.ActionLink("Edit", "Edit", new { id = item.MReadingId }) |
                        @Html.ActionLink("Details", "Details", new { id = item.MReadingId }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.MReadingId })*@
                    </td>
                </tr>
            }
        </tbody>
    </table>
    </div>